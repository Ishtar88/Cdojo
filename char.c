#include <stdio.h>

int main20()
{
    char c1 = 'a';
    char c2 = 'b'; 

    //char를 %c로 출력하면 문자가 출력되고, %d로 출력하면 정숫값이 출력됨 

    printf("%c, %d \n", c1, c1);
    printf("%c, %d\n", c2, c2);

    return 0;
}